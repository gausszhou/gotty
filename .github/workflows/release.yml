name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    runs-on: ubuntu-latest
    # 创建 Release 与生成发布说明需要 contents: write;仓库默认 token 是
    # 只读,不显式授权会报 "Resource not accessible by integration"。
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Configure pnpm store
        # 显式固定 store-dir,并创建 pnpm store path 解析出的精确目录
        # (store-dir 会有 /v3 版本子目录),确保 setup-node 的缓存
        # 路径解析成功,避免 "Some specified paths were not resolved"
        run: |
          pnpm config set store-dir "${{ runner.temp }}/pnpm-store"
          mkdir -p "$(pnpm store path)"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: make install

      - name: Build all platforms
        # make release 产出 5 平台矩阵的原始二进制 + 压缩包 + sha256sums.txt
        run: make release

      - name: Verify binary
        run: ./build/gotty-linux-amd64 --version

      - name: Verify sha256sums
        # 回归:资产与校验和一致(install.sh / self update 都依赖校验和)
        run: |
          cd build && sha256sum -c --ignore-missing sha256sums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          # git log 自动生成发布说明
          generate_release_notes: true
          files: |
            build/gotty-linux-amd64
            build/gotty-linux-arm64
            build/gotty-darwin-amd64
            build/gotty-darwin-arm64
            build/gotty-windows-amd64.exe
            build/gotty-linux-amd64.tar.gz
            build/gotty-linux-arm64.tar.gz
            build/gotty-darwin-amd64.tar.gz
            build/gotty-darwin-arm64.tar.gz
            build/gotty-windows-amd64.zip
            build/sha256sums.txt
